------------2025.4.21-----------
后端同学（刘佳凡）问题：
能不能就在这个里面实现前后端，这样我们后端同学就直接用python去调用模型来实现，然后flask包装接口，前端就是直接改这个，这样感觉大家都不用新去学那个app开发

感觉你上次manus应该是完全可以用的，只有一个index.html文件有问题，最后太长没生成完，应该最后有一段把它所有js文件包含进去，让他把剩下的生成完呗。就是包含<script src="static/index.js"></script>各个js文件这种，应该就可用了

回答：
manus生成一次要半个小时，而且很贵

------------2025.5.19-----------
工作进度：
前端开发：目前看下来就是 前端<-api->大模型
后端开发：使用扁鹊模型为基础模型，再Chinese medical dialogue data 中文医疗问答数据集上又进行微调，用streamlit_chat简单实现了网页版前后端交互
部署：配置部署环境，这是Android studio的SDK版本


------------2025.5.25-----------
前端重写了一下，现在就是一个聊天端口调黄杨的硅基流动的api

------------2025.6.23-----------
目前存在的问题：
日历这块有几个问题：
1.打开设置的这个地方跳转的逻辑bug，具体看视频里出现的情况
2.时间选不了，只能选日期，这块是没做还是做错了我不清楚，一定要改了，而且这样显示不是很友好
3.通过日历选择日期这块显示的bug
这几个问题一定要解决掉@🍉 @Kepler 
然后来个测试组的同学把这块认领一下，把这里出现的情况写成一个issue再GitHub里提出来

对话这块也有问题：
文字输出的时候应该是流式输出（一个字一个字或几个字几个字的蹦出来）而不是非流式的，所有文字全部得到以后才一起输出会让用户觉得响应太慢，效果看起来是不好的，所以要改成stream模式@仰望 丶 暒涳 @王博海 
同样，来个测试的同学把这里的情况写成issue提到GitHub上


